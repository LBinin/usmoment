# Third-Party Notices

`@usmoment/icon` tracks icon sources per icon.

## Project-Provided Icons

### BackspaceIcon

- Source: project maintainer-provided inline SVG data URL.
- License: project-provided.
- Notes: this first icon preserves the original SVG geometry, but uses
  `currentColor` by default so consumers can theme it consistently.

### DateIcon

- Source: project maintainer-provided local SVG file.
- License: project-provided.
- Notes: rebuilt as structured SVG path data with color controlled by
  `currentColor`.

### ImageIcon

- Source: project maintainer-provided local SVG file.
- License: project-provided.
- Notes: rebuilt as structured SVG path data with color controlled by
  `currentColor`.

### NoteIcon

- Source: project maintainer-provided local SVG file.
- License: project-provided.
- Notes: rebuilt as structured SVG path data with color controlled by
  `currentColor`.

### PlusIcon

- Source: project maintainer-provided local SVG file.
- License: project-provided.
- Notes: rebuilt as structured SVG path data with color controlled by
  `currentColor`; the original fill opacity was intentionally removed.

### TimeIcon

- Source: project maintainer-provided local SVG file.
- License: project-provided.
- Notes: rebuilt as structured SVG path data with color controlled by
  `currentColor`.

### YenCircleIcon

- Source: project maintainer-provided inline SVG data URL.
- License: project-provided.
- Notes: this icon preserves the original SVG geometry, but uses `currentColor`
  by default so consumers can theme it consistently.
