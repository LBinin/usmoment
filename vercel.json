{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build:docs",
  "outputDirectory": "docs/site-react/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
